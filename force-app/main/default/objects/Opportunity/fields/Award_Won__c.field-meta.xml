<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Award_Won__c</fullName>
    <description>1 when the grant was won, 0 otherwise. Exists only so reports can compute a success rate: a report cannot count rows conditionally, so success rate by funder is Award Won:SUM / Record Count with the report filtered to Closed = True. Reporting helper - deliberately kept off page layouts.</description>
    <externalId>false</externalId>
    <formula>IF(IsWon, 1, 0)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Award Won</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
