<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>LOI_Submitted_Date__c</fullName>
    <description>Date the Letter of Intent was submitted to the funder. Distinct from Close Date (award decision) and from later proposal dates. Populated when an LOI is sent; leave blank when the funder does not use an LOI. Denominator for LOI Success Rate. Do not clear after a decline - the submission still happened.</description>
    <label>LOI Submitted Date</label>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>true</trackHistory>
    <type>Date</type>
</CustomField>
