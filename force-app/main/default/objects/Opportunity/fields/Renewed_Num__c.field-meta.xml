<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Renewed_Num__c</fullName>
    <description>Numeric mirror of the Renewed checkbox (1 = renewed, 0 = not). Exists so the Grant Renewal Rate report can sum renewals in a custom summary formula (checkboxes and report row-level formulas cannot be summed in a summary formula).</description>
    <externalId>false</externalId>
    <formula>IF(Renewed__c, 1, 0)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Renewed (Numeric)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
